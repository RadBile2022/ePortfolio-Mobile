import 'package:flutter/material.dart';

class LockIcon12 extends StatelessWidget {
  const LockIcon12({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.lock,
      size: 12,
      color: Colors.grey,
    );
  }
}
